version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd blog_frontend
        - npm ci
    build:
      commands:
        - npm run build

  artifacts:
    baseDirectory: blog_frontend/dist
    files:
      - '**/*'

  cache:
    paths:
      - blog_frontend/node_modules/**/*