version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Blog
        - npm install
    build:
      commands:
        - cd Blog
        - npm run build
  artifacts:
    baseDirectory: Blog/dist
    files:
      - '**/*'
  cache:
    paths:
      - Blog/node_modules/**/*